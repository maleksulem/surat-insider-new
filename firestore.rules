rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All collections are accessed exclusively by the server-side Express backend.
    // The client-side frontend does not interact with Firestore directly.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
